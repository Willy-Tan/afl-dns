(executables
 ((names (test))
  (libraries (dns oUnit pcap-format crowbar))
  (preprocess (pps (ppx_cstruct)))
))
(alias
 ((name    runtest)
  (deps    (test.exe (glob_files *.pcap) (glob_files *.zone)))
  (action  (run ${<}))))


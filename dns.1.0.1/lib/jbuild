(library
 ((name        dns) 
  (public_name dns)
  (libraries   (crowbar re re.str ipaddr uri uri.services base64 hashcons cstruct result))
  (preprocess (pps (ppx_cstruct ppx_deriving_crowbar)))
))
(ocamllex (zone_lexer))
(ocamlyacc (zone_parser))

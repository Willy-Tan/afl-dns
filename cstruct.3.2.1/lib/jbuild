(library
  ((name cstruct)
   (public_name cstruct)
   (modules (cstruct))
   (wrapped false)
   (libraries (sexplib crowbar))
   (preprocess (pps (ppx_deriving_crowbar)))
   (c_names (cstruct_stubs))
))
(jbuild_version 1)
